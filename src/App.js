import "./App.css";
import Input from "./components/Input/input";

const App = () => {
  return (
    <div className="App">
      <h1>Hello there</h1>
      <Input inputSubmitHandler={() => {}} inputChangeHandler={() => {}} />
    </div>
  );
};

export default App;
